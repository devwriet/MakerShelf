# MakerShelf 팀 Git 가이드

이 문서는 Git이 처음인 팀원도 따라 할 수 있도록 만든 가이드예요. 순서대로 읽고, 막히는 부분이 있으면 리더에게 바로 물어보세요.

## 1. 팀 규칙 한눈에 보기

- `main` 브랜치(기본 브랜치)를 직접 수정하지 않아요. 항상 기능 브랜치에서 작업해요.
- 작업이 끝나면 PR(Pull Request, 풀 리퀘스트)을 올려요.
- 리더가 코드 리뷰를 한 뒤, `main`에 머지(병합)해요.
- `main`에는 절대 강제 푸시(`--force`)하지 않아요. `--force`가 필요할 것 같으면 먼저 리더에게 물어봐요.

## 2. 용어와 저장 위치

작업한 내용이 실제로 GitHub에 올라가기까지 이렇게 4단계를 거쳐요.

```text
작업 폴더 → git add → 스테이징 영역 → git commit → 로컬 저장소 → git push → GitHub 원격 저장소
```

- **저장소(레포)**: 프로젝트 전체 기록이 담긴 곳. 내 컴퓨터에 있으면 **로컬 저장소**, GitHub에 있으면 **원격 저장소**예요.
- **브랜치**: 작업을 나누는 별도 공간이에요. `main`은 완성된 코드만 있는 기본 브랜치, 각자 작업은 **기능 브랜치**에서 해요.
- **커밋**: 변경 사항의 스냅샷을 로컬 저장소에 기록하는 것. 아직 GitHub에는 안 올라간 상태예요.
- **푸시**: 로컬에 쌓인 커밋을 원격 저장소로 보내는 것.
- **PR**: "이렇게 고쳤으니 `main`에 합쳐도 될까요?"라고 올리는 리뷰/병합 요청이에요.
- **머지(병합)**: 리뷰가 끝난 브랜치를 `main`에 합치는 것.

## 3. 처음 한 번: 클론과 초기 설정

```bash
git clone <저장소 URL>
cd MakerShelf
```

- 아래 명령으로 본인 정보가 이미 설정되어 있는지 확인해요. 아무것도 안 뜨면 아직 설정 안 된 거예요.
  ```bash
  git config --global user.name
  git config --global user.email
  ```
  비어 있다면 아래처럼 설정해요 (한 번만 하면 돼요).
  ```bash
  git config --global user.name "이름"
  git config --global user.email "이메일"
  ```
- `.gitignore`에 이미 등록된 파일(빌드 결과물, IDE 설정 등)은 신경 쓰지 않아도 돼요. 새로운 종류의 파일을 커밋하기 전에 `.gitignore`에 추가해야 하는지 먼저 확인해요.

## 3-1. GitHub 이슈 템플릿

GitHub의 "New issue"를 누르면 두 가지 템플릿 중에 고를 수 있어요.

- **계획된 로드맵 작업**(ROADMAP.md의 각 Phase) → "단계(Phase) 이슈" 템플릿
- **코드에서 발견한 버그** → "버그 신고" 템플릿

둘 다 안 맞는 사소한 건 굳이 이슈로 안 만들어도 돼요 — 팀 채팅으로 물어보거나 PR 코멘트로 남기는 걸로 충분해요. 빈 이슈(템플릿 없이)도 막혀있지 않으니, 정말 둘 다 안 맞으면 그냥 만들어도 괜찮아요.

## 4. 기능 작업의 표준 흐름

새 작업을 시작할 때마다 이 순서를 따라요.

```bash
git checkout main
git pull                        # main을 최신 상태로 업데이트
git checkout -b feature/작업이름   # 새 기능 브랜치 생성
```

작업하는 동안:

```bash
git status                      # 지금 어떤 브랜치에 있는지, 뭐가 바뀌었는지 확인
git add -p 파일경로               # 변경 사항을 스테이징 영역에 올리기 (조각 단위로 확인하며 올림)
git commit -m "커밋 메시지"        # 스테이징된 내용을 로컬 저장소에 기록
git push -u origin feature/작업이름   # 로컬 커밋을 원격 저장소로 전송 (-u는 이 브랜치를 처음 올릴 때만 필요, 이후엔 git push만 써도 돼요)
```

**끝난 뒤 상태**: 로컬 커밋이 원격 저장소의 같은 이름 브랜치에 올라가 있어요. `main`은 아직 그대로예요.

**자주 하는 실수**:
- `git add`만 하고 `git commit`을 안 해서 "분명 add 했는데 커밋에 안 보여요" — `add`는 스테이징만 하고, `commit`이 실제로 기록해요.
- `commit`만 하고 `push`를 안 해서 "커밋했는데 GitHub에 안 보여요" — `commit`은 로컬에만 저장돼요. `push`해야 GitHub에 올라가요.
- 파일을 또 수정했는데 `add`를 다시 안 해서 이전 버전만 커밋되는 경우 — 수정할 때마다 `git add`를 다시 실행해요.

## 4-1. 브랜치는 backend/ 또는 frontend/ 한쪽만 (기본 규칙)

이 저장소는 `backend/`(Spring Boot)와 `frontend/`(React)가 한 저장소 안에 있는 모노레포예요. 기본적으로 브랜치 하나 = 폴더 하나만 건드려요.

- `feature/backend-공급자-api` — `backend/`만 수정
- `feature/frontend-공급자-화면` — `frontend/`만 수정

**왜 이렇게 하나요?**
- 백엔드 담당자는 백엔드 PR만, 프론트엔드 담당자는 프론트엔드 PR만 리뷰하면 되니까 자기가 아는 코드만 보고 승인할 수 있어요.
- 다른 사람이 작업 중인 파일과 겹칠 일이 적어서 충돌이 줄어요.
- 예외: 풀스택 담당자가 혼자 작은 기능을 처음부터 끝까지 만들 때는 두 폴더를 한 브랜치에서 같이 작업해도 괜찮아요.

**같은 기능이 backend/frontend 양쪽에 걸쳐 있으면** (예: "공급자 등록" 기능) 브랜치/PR을 두 개로 나누고, 아래 5-1의 API 계약으로 서로 연결해요.

**프론트엔드는 Figma 디자인이 먼저 나와요.** 화면 작업은 해당 화면의 Figma 디자인이 완성된 뒤에 `feature/frontend-...` 브랜치를 시작해요. 디자인 확인은 리더가 담당해요 — 디자인이 아직 없다면 리더에게 먼저 문의하고, 리더 확인을 받은 뒤 구현을 시작하세요. 확인은 말로만 하지 말고, Figma 디자인 파일에 "승인" 코멘트를 날짜와 함께 남겨두면 나중에 언제 확인됐는지 헷갈리지 않아요. PR 설명에 참고한 Figma 링크를 남기면 리뷰할 때 비교하기 편해요.

## 4-2. 커밋 메시지 규칙

커밋 메시지는 이 형식으로 써요.

```
<Action>: <한 줄 요약>
```

- **Action**: 아래 9개 중 하나 (영어 단어, 첫 글자 대문자)

| Action | 언제 쓰나요 |
| --- | --- |
| `Add` | 새 기능/파일 추가 |
| `Update` | 기존 기능 수정 (아래 태그에 안 맞는 일반적인 수정) |
| `Fix` | 버그 수정 |
| `Remove` | 코드/파일 삭제 |
| `Refactor` | 동작은 그대로, 코드 구조만 개선 |
| `Docs` | 문서(README, 주석 등)만 수정 |
| `Test` | 테스트 코드 추가/수정 |
| `Build` | 의존성, 설정, CI 등 빌드 관련 변경 |
| `Style` | 포맷팅 등 코드 동작에 영향 없는 스타일 변경 |

- **요약**: 한 줄로 무엇을 했는지 (개조식, `~함/~추가/~수정` 형태)

예시:
```
Add: 공급자 등록 API 추가
Fix: 재고 수량 계산 오류 수정
Test: 재고 수량 계산 서비스 단위 테스트 추가
Docs: README에 실행 방법 추가
```

## 5. PR 생성

기능 브랜치를 다 작업했고 원격 저장소에 푸시했다면, GitHub에서 PR을 올려요.

- **base**: 합쳐질 브랜치 → `main`
- **compare**: 내 변경 사항이 있는 브랜치 → `feature/작업이름`

PR을 새로 만들면 아래 항목이 적힌 템플릿이 자동으로 채워져요. 필수 항목만 채우고, 해당 안 되는 항목은 비워둬도 돼요.
- 무엇을 바꿨는지 (필수)
- Figma 링크 (프론트엔드 화면 작업일 때만)
- API 계약 (백엔드/프론트엔드 연동이 있을 때만)
- 어떻게 확인/테스트했는지 (필수)
- 리뷰어가 특히 봐줬으면 하는 부분 (필수는 아니지만 있으면 리뷰가 빨라져요)

## 5-1. API 계약 작성 예시

백엔드 PR과 프론트엔드 PR이 짝을 이룰 때는, 작업을 시작하기 전에 API 계약(엔드포인트/요청/응답/에러)을 먼저 정하고 두 PR 본문에 똑같이 적어요. 형식이 헷갈리면 이 예시를 그대로 따라 써도 돼요.

```
- 엔드포인트: POST /api/suppliers
- 요청: { name: string, contact: string }
- 응답: { id: number, name: string, contact: string, createdAt: string }
- 에러: 400 (name 누락), 409 (같은 이름 이미 존재)
```

백엔드 PR이 먼저 이 계약대로 API를 만들고, 프론트엔드 PR은 이 계약을 보고 화면을 만들어요. 백엔드가 아직 머지되지 않았다면, 이 계약대로 가짜 데이터(mock)를 만들어서 먼저 화면 작업을 진행해도 돼요. 계약이 바뀌면 두 PR 본문을 같이 수정해요.

## 6. 코드 리뷰와 수정 반영

- 리더가 PR의 `Files changed` 탭에서 코드를 확인하고, `Comment`(코멘트), `Approve`(승인), `Request changes`(수정 요청) 중 하나로 리뷰해요.
- 코멘트가 달리면, 같은 기능 브랜치에서 수정한 뒤 다시 `커밋 → 푸시`하면 돼요. 같은 PR이 자동으로 업데이트되니 새 PR을 또 만들 필요는 없어요.
- 리뷰 코멘트는 코드에 대한 의견이에요. 필요하면 이유를 답글로 남기고, 수정한 뒤 리뷰를 다시 요청해요.
- `Resolve conversation`은 실제로 고치거나 논의가 끝난 뒤에 눌러요.
- 승인이 끝나면 리더가 `main`에 머지해요. (이 저장소에서는 리더만 머지해요.)

## 7. 작업 완료 후 최신 main 받기

PR이 머지된 뒤에는 이렇게 정리해요.

```bash
git checkout main
git pull
git branch -d feature/작업이름     # 머지된 로컬 브랜치 삭제
```

## 8. 충돌 기초 해결

**충돌이 뭔가요?** 같은 파일의 같은 부분을 두 브랜치에서 다르게 고쳤을 때, Git이 어느 쪽이 맞는지 스스로 판단할 수 없어서 생기는 상황이에요. 프로젝트가 망가진 게 아니고, 다른 팀원의 브랜치가 손상되는 것도 아니에요 — 지금 머지/풀을 하고 있는 사람만 해결하면 돼요.

충돌은 보통 내 기능 브랜치에 최신 `main`을 반영할 때 생겨요. 먼저 최신 `main`을 내 브랜치로 가져와요.

```bash
git checkout feature/작업이름        # 내 기능 브랜치로 이동
git fetch origin                   # 원격 저장소의 최신 정보를 가져옴
git merge origin/main              # main의 최신 내용을 내 브랜치에 합침 (여기서 충돌이 날 수 있음)
```

여기서 충돌이 나면 아래 순서로 해결해요.

```bash
git status                         # 충돌 난 파일 확인
# 충돌 표시가 있는 파일을 열고 최종 내용을 직접 정함
git add 해결한파일경로               # 이 파일은 해결됐다고 표시
git status                         # 더 이상 충돌 파일이 없는지 확인
git commit -m "Resolve merge conflict"
git push origin feature/작업이름     # PR 브랜치였다면 다시 푸시해서 PR 갱신
```

파일 안의 충돌 표시는 이렇게 생겼어요.

```text
<<<<<<< HEAD
지금 내 브랜치의 내용
=======
가져오려는 브랜치의 내용
>>>>>>> feature/x
```

`<<<<<<<`, `=======`, `>>>>>>>` 표시를 지우고, 둘 중 하나를 선택하거나 둘을 합쳐서 최종 내용을 정한 뒤 저장해요. 어느 쪽이 맞는지 모른 채로 아무거나 고르지 말고, 확실하지 않으면 리더에게 물어봐요. 진행 중인 머지를 취소하고 싶으면 `git merge --abort`로 되돌릴 수 있어요.

## 9. 금지/주의 사항

- `main`에 직접 푸시하지 않아요. 항상 기능 브랜치 → PR 순서예요.
- `--force`는 원격 브랜치 기록을 덮어써서 다른 사람의 커밋을 지울 수 있어요. 사용하기 전에 반드시 리더에게 확인해요.
- 비밀번호, API 키, `.env` 파일 같은 민감한 정보는 커밋하지 않아요. `git add` 하기 전에 어떤 파일이 올라가는지 `git status`로 확인하는 습관을 들여요.

## 10. 자주 쓰는 명령어 모음

| 명령어 | 설명 |
| --- | --- |
| `git status` | 현재 브랜치와 변경 사항 확인 |
| `git branch` | 로컬 브랜치 목록 (`*`가 현재 브랜치) |
| `git diff` | 아직 스테이징 안 된 변경 내용 확인 |
| `git diff --staged` | 스테이징된 변경 내용 확인 |
| `git add -p 파일` | 변경 사항을 조각 단위로 스테이징 |
| `git commit -m "메시지"` | 스테이징된 내용을 로컬에 커밋 |
| `git push origin 브랜치명` | 로컬 커밋을 원격 저장소로 전송 |
| `git pull` | 현재 브랜치에 원격 변경 사항 반영 |
| `git checkout -b 브랜치명` | 새 브랜치 생성 후 이동 |
| `git merge --abort` | 진행 중인 머지 취소 |

## 11. 문제 해결 / FAQ

**`push`가 `non-fast-forward`로 거절돼요.**
보통 누군가 먼저 푸시해서 원격 브랜치가 내 로컬보다 앞서 있는 상태예요 (드물게는 `git commit --amend`처럼 이미 푸시한 커밋을 직접 고쳤을 때도 나요). 먼저 `git pull`로 원격 변경 사항을 가져와 합친 뒤 다시 `push`하세요. 곧바로 `--force`를 쓰지 마세요 — 정말 필요한 상황(예: 나만 쓰는 브랜치를 의도적으로 고쳤을 때)이라면 팀 규칙 9번대로 먼저 리더에게 확인해요.

**`git commit -am`으로 커밋했는데 방금 새로 만든 파일이 안 들어갔어요.**
`-a`는 Git이 이미 추적 중인 파일의 수정/삭제만 자동으로 스테이징해요. 새로 만든 파일(아직 한 번도 커밋 안 된 파일)은 `git add 파일명`으로 직접 추가해야 해요.

**"detached HEAD" 라는 메시지가 떠요.**
브랜치가 아니라 특정 커밋을 직접 체크아웃했을 때 나와요. 작업이 사라지는 건 아니지만, 이 상태에서 커밋하면 어느 브랜치에도 안 붙어요. 원래 작업하던 브랜치로 `git checkout 브랜치명`으로 돌아가세요. 이 상태에서 이미 커밋을 했다면, `git switch -c 임시브랜치명`으로 지금 위치에 새 브랜치를 만들어서 작업을 구제할 수 있어요.

**`git pull`과 `git fetch`의 차이를 모르겠어요.**
`fetch`는 원격 저장소의 최신 정보만 받아오고 내 브랜치는 건드리지 않아요. `pull`은 `fetch` + 내 브랜치에 반영까지 한 번에 하는데, 이 저장소에서는 보통 합치기(merge)로 반영돼요. 먼저 확인만 하고 싶으면 `fetch`, 바로 반영해도 되면 `pull`을 써요.

**실수로 `main`에 직접 커밋했어요.**
아직 푸시 전이라면: 먼저 `git status`로 지금 다른 작업 중인(아직 커밋 안 한) 변경 사항이 없는지 확인하세요 — 있다면 `git reset --hard`가 그 변경 사항까지 지울 수 있어요. 정리됐다면 `git checkout -b feature/작업이름`으로 지금 위치에 새 브랜치를 만든 뒤, `git checkout main`, `git fetch origin`, `git reset --hard origin/main`으로 `main`을 원래 상태로 되돌리세요 (되돌리기 전에 꼭 리더에게 먼저 알려요). 이미 푸시까지 했다면 되돌리지 말고 리더에게 바로 알려요.

**마지막 커밋 메시지를 잘못 썼어요.**
아직 `push`하지 않았다면 `git commit --amend -m "고친 메시지"`로 바로 고칠 수 있어요. 이미 `push`한 뒤라면 함부로 고치지 말고(다른 사람이 이미 받아갔을 수 있어요), 리더에게 알린 뒤 방법을 정해요.

**`.gitignore`에 추가했는데 파일이 계속 보이거나 커밋돼요.**
`.gitignore`는 아직 Git이 추적하고 있지 않은 파일에만 적용돼요. 이미 한 번이라도 커밋된 적 있어서 추적 중인 파일은 `.gitignore`에 넣어도 안 통해요. 이 경우 `git rm --cached 파일명`으로 추적만 해제한 뒤(파일 자체는 로컬에 남아있어요) 커밋해야 그때부터 `.gitignore`가 효과를 발휘해요. 비밀번호나 API 키가 포함된 파일이었다면, 추적 해제만으로는 부족해요 — 해당 값을 즉시 변경(rotate)하고 리더에게 알리세요.

**`git checkout -b 브랜치명`이 "이미 존재하는 브랜치예요"라고 해요.**
`-b`는 "새로 만들기"라는 뜻이에요. 이미 만든 적 있는 브랜치라면 `-b` 없이 `git checkout 브랜치명`으로 그냥 이동하면 돼요.

**튜토리얼에서 `git push --force`를 쓰라는데 위험해 보여요.**
맞아요, 위험해요. `--force`는 원격 브랜치를 내 로컬 상태로 강제로 덮어써서 팀원의 커밋을 지울 수 있어요. 정말 필요한 상황이라도 먼저 리더에게 확인하세요 (팀 규칙 9번 참고).

막히면 참고 서면으로 끝내지 말고 팀 채팅이나 리더에게 바로 물어보세요.

---

GUI(GitHub Desktop)를 쓴다면 [CONTRIBUTING-desktop.md](./CONTRIBUTING-desktop.md)를 참고하세요 — 같은 팀 규칙을 GitHub Desktop 화면 기준으로 안내해요.
