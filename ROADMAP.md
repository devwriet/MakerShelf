# MakerShelf 구현 로드맵

카페 위탁판매 재고 관리 시스템의 기능 구현 순서와 백엔드/프론트엔드 병렬 작업 방식을 정리한 문서예요. 팀 상황(백엔드 1명, 프론트엔드 2명, 리더는 양쪽 다 가능)에 맞춰 설계했어요.

## 작업 방식

각 단계(Phase)마다 이 순서를 따라요.

1. **API 계약을 먼저 정해요** (CONTRIBUTING.md 5-1 방식). 구체적인 절차: (1) 리더가 그 단계의 GitHub 이슈를 만들어요 — **"New issue" 시 "단계(Phase) 이슈" 템플릿**([.github/ISSUE_TEMPLATE/phase-issue.md](./.github/ISSUE_TEMPLATE/phase-issue.md))을 골라요. (2) 백엔드 담당자가 그 이슈에 엔드포인트, 필드/타입, 검증 규칙, 성공/에러 응답 예시를 초안으로 작성해요. (3) 백엔드·프론트엔드 담당자가 이슈에서 승인해요. (4) 리더가 이슈 제목에 **[확정]**을 붙이면 — 그때부터가 "계약이 정해진" 시점이에요, 그 전에는 구현을 시작하지 마세요. 화면 담당자는 이 이슈에 적힌 계약 + 승인된 Figma를 확인하는 게 첫 번째 할 일이에요.
2. 계약이 정해지면 **백엔드는 구현을 시작**하고, **프론트엔드 2명은 같은 단계 안에서 작업을 나눠서 동시에 진행**해요. 다만 "화면 두 개 = 항상 똑같은 크기"라고 가정하지 마세요 — 목록/조회 화면이 등록 폼보다 훨씬 클 때도 있고(예: Phase 3의 재고 목록), 반대로 한쪽 화면이 너무 작아서 혼자 하기 부담스러울 때도 있어요(예: Phase 4의 폐기 기록). 각 단계 시작 시점에 리더가 작업을 실제 분량 기준으로 나누고(화면 구현 + API 연동 + 에러/빈 상태 처리 + 테스트까지 포함), 누가 뭘 맡을지 이름으로 정해두세요. 백엔드가 아직 머지 전이면 계약대로 가짜 데이터(mock)를 써서 먼저 화면을 만들어요.
3. 백엔드는 프론트엔드보다 **한 단계 앞서** 다음 단계의 계약을 준비해요. 다만 이건 "백엔드가 알아서 두 가지 일을 동시에 한다"는 막연한 가정이 아니라, **각 단계가 끝날 무렵 리더 주도로 다음 단계 계약 회의를 짧게 잡는 것**으로 실행해요 — 이렇게 해야 실제로 지켜져요.
4. 프론트엔드 화면 작업은 항상 **해당 화면의 Figma 디자인이 먼저 완성된 뒤**에 시작해요 (CONTRIBUTING.md 4-1). Figma도 프론트엔드 작업보다 한 단계 앞서 준비되어 있어야 이 규칙이 병목이 안 돼요.

### 단계 종료 체크리스트

**"단계(Phase) 이슈" 템플릿에 이 체크리스트가 이미 포함되어 있어요** — 따로 옮겨 적을 필요 없이 그 이슈에서 바로 체크하면 돼요. 각 담당자가 자기 항목에 근거(PR 링크 등)를 남기고, **리더가 최종 확인 후 이슈를 닫으면 그 단계가 "완료"**예요 — 개인이 임의로 완료 처리하지 않아요.

- [ ] 담당자(백엔드/프론트엔드 각각)가 정해져 있었다
- [ ] Figma 디자인 승인 완료 (프론트엔드 작업 대상 화면 전부)
- [ ] API 계약 확정([확정] 이슈) 및 양쪽 PR에 기록
- [ ] 백엔드 PR 머지 (테스트 포함)
- [ ] 프론트엔드 PR 머지 (mock 제거하고 실제 API 연동 확인)
- [ ] 다음 단계 API 계약 초안 작성 시작 (다음 단계 이슈 생성)

## 단계별 로드맵

### Phase 0 — 앱 셸 / 인덱스 페이지 (진행 중)

- **프론트엔드**: 인덱스 페이지 Figma 디자인 완료 후 구현 — 레이아웃/내비게이션 셸 포함. 백엔드 의존성 없음 (기존 `/api/health`만 확인용으로 씀).
- **백엔드**: 이 단계와 별개로 Phase 0.5, Phase 1 준비를 시작해요.

### Phase 0.5 — Flyway 도입 + 기본 CI (백엔드 전용, 짧게)

- 실제 도메인 엔티티가 Phase 1부터 생기기 시작하니, 그 전에 마이그레이션 도구를 넣어요. `ddl-auto: none`인 채로 실제 테이블이 생기면 팀원마다 로컬 스키마가 어긋날 위험이 커요.
- Flyway 의존성 추가, `db/migration/` 폴더 구조 잡기, 빈 베이스라인 마이그레이션 하나 작성.
- **기본 CI도 이때 같이 넣어요** — Oracle 연결이 필요한 통합 테스트까지는 아직 무리지만, DB 없이 돌아가는 프론트엔드 build/lint와 백엔드 unit/service 테스트는 지금 바로 자동화할 수 있어요. PR마다 최소한 "빌드는 되는지, 유닛 테스트는 통과하는지"는 자동으로 확인되게 해요.
- 프론트엔드 작업에는 영향 없음 — Phase 0과 병렬로 진행.

### Phase 1 — 창작자 (Creator)

- **API 계약**: `GET/POST /api/creators`, `GET/PUT /api/creators/{id}` (필드: 이름, 연락처 등 — 계약 작성 시 확정)
- **백엔드**: Creator 엔티티, 리포지토리, CRUD API, 첫 Flyway 마이그레이션(creators 테이블)
- **백엔드**: 로컬 개발용 시드 데이터 스크립트도 이때 같이 만들어요 (샘플 창작자 몇 개) — 팀원마다 로컬 DB가 따로 있으니, 같은 테스트 데이터로 맞춰볼 수 있는 간단한 방법이 필요해요. 거창할 필요 없이 Flyway 마이그레이션이나 간단한 SQL 스크립트 하나면 충분해요.
- **프론트엔드**: 창작자 목록/조회 화면 (담당 1명) + 창작자 등록/수정 폼 (담당 1명)

### Phase 2 — 위탁계약 (Consignment Contract)

- Creator에 의존 (외래키)
- **API 계약**: `GET/POST /api/contracts`, `GET/PUT /api/contracts/{id}` (창작자 참조 포함)
- **백엔드**: Contract 엔티티, CRUD API, 마이그레이션, 시드 데이터에 샘플 계약 추가
- **프론트엔드**: 창작자별 계약 목록 화면 (담당 1명) + 계약 등록/수정 폼 (담당 1명)
- **이 단계 후반부터 Settlement(Phase 5) 설계 스파이크를 시작해요** — 구현은 Phase 5에서 하지만, 설계는 미리 정리해둬야 Sale/Disposal 계약을 나중에 다시 고치는 일이 없어요. **결과물(완료 기준)**: 아래 항목을 담은 문서 하나(`docs/settlement-design.md` 등)를 만들고 리더 + 백엔드 담당자가 리뷰/승인해요. 이 문서가 나와야 "스파이크 완료"예요.
  - 정산 기간의 시작/끝을 어떻게 정할지 (타임존 포함)
  - 어떤 판매/폐기 기록이 그 기간의 집계 대상인지
  - 수수료 계산 방식, 반올림 규칙
  - 스냅샷(정산명세서)에 실제로 어떤 필드가 들어가는지
  - 같은 기간을 실수로 두 번 집계해도 안전한지(멱등성 — 같은 작업을 여러 번 반복해도 결과가 달라지지 않는다는 뜻)
  - 정산이 끝난 뒤에 판매/폐기 기록을 고쳐야 하면 어떻게 처리할지

### Phase 3 — 입고배치 (Intake Batch)

- Contract에 의존
- **API 계약**: `GET/POST /api/intake-batches`, `GET /api/intake-batches/{id}` (계약 참조, 수량, 입고일 등)
- **백엔드**: IntakeBatch 엔티티, CRUD API, 마이그레이션
- **프론트엔드**: 재고/입고 목록 화면 (담당 1명) + 입고 등록 폼 (담당 1명)

### Phase 4A — 판매 (Sale)

IntakeBatch에 의존. 재고를 실제로 변경하는 로직이 여기서 처음 생기는 단계라 매출 관련 규칙(수량 검증, 재고 원자적 차감, 감사 로그 등)을 여기서 확립해요 — Disposal이 이 패턴을 그대로 재사용해요.

- **API 계약**: `POST /api/sales`, `GET /api/sales` (입고배치 참조, 수량, 판매가)
- **백엔드**: Sale 엔티티, API, 재고 차감 로직(원자적 처리 + 감사 로그), 마이그레이션
- **프론트엔드**: 판매 기록 입력 화면 (담당 1명) + 판매 내역 목록 화면 (담당 1명)

### Phase 4B — 폐기기록 (Disposal)

IntakeBatch에 의존. Sale보다 훨씬 단순하지만(매출 계산 없음), **수량 검증/원자적 재고 차감/감사 로그는 Phase 4A에서 만든 것과 반드시 같은 방식으로** 구현해요 — 여기서 따로 다른 패턴을 새로 만들지 마세요. (Phase 4A가 끝나면 이 문단에 실제 클래스/서비스 이름과 테스트 경로를 링크로 추가하세요. 만약 재사용할 게 애매하면, 4B 시작 전에 재고 차감 로직을 공용 컴포넌트로 먼저 분리하세요.)

- **API 계약**: `POST /api/disposals`, `GET /api/disposals`
- **백엔드**: Disposal 엔티티, API, 마이그레이션
- **프론트엔드**: 폐기 기록 입력 화면 (담당 1명) + 폐기 내역 목록 화면 (담당 1명) — 목록 화면이 빠졌던 걸 추가했어요, 판매 내역처럼 나중에 조회할 수 있어야 해요

### Phase 5 — 정산명세서 (Settlement Statement)

- Contract + Sale + Disposal에 의존. 특정 기간의 판매/폐기 내역을 창작자별로 집계해서 **불변(immutable) 스냅샷**으로 저장 — 가장 복잡한 단계, 백엔드 비중이 커요.
- **API 계약**: `POST /api/settlements` (정산 생성/집계 트리거), `GET /api/settlements`, `GET /api/settlements/{id}`
- **백엔드**: Settlement 엔티티(스냅샷, 수정 불가), 집계 로직, API, 마이그레이션
- **프론트엔드**: 정산 목록 화면 (담당 1명) + 정산 상세/내역 화면 (담당 1명)

## 이 로드맵에 없는 것 (의도적으로 보류)

- **인증/로그인**: 이번 프로젝트 범위에서는 안 만들어요 (2026-09-04 결정, 도메인 구현에 집중).
- **Oracle 연결이 필요한 통합 테스트를 CI에 넣는 것**: 기본 CI(빌드/lint/유닛테스트)는 Phase 0.5부터 바로 넣지만, 실제 Oracle DB에 붙는 통합 테스트를 CI에서 돌리는 방법은 나중에 다시 검토해요 (팀 공용 CI용 DB가 필요한 문제라 지금 당장 풀 필요는 없어요).

## 용어 설명

이 문서에 나오는 낯설 수 있는 용어들이에요.

- **Flyway**: DB 스키마(테이블 구조)를 파일로 기록해서 버전 관리하는 도구예요. "테이블을 이렇게 바꿔라"는 SQL 파일을 순서대로 쌓아두면, 누가 실행하든 같은 순서로 같은 결과가 나와요.
- **`ddl-auto: none`**: Spring Boot(JPA/Hibernate)가 코드 보고 알아서 테이블을 만들거나 바꾸지 않는다는 설정이에요. 즉 테이블 구조는 오직 Flyway로만 바뀌어요.
- **CI (Continuous Integration)**: PR을 올릴 때마다 자동으로 빌드/테스트를 돌려주는 장치예요. 사람이 일일이 "내 컴퓨터에서는 잘 됐어요"라고 안 해도, 기계가 확인해줘요.
- **mock (가짜 데이터)**: 백엔드 API가 아직 안 끝났을 때, 프론트엔드가 화면을 먼저 만들 수 있도록 정해진 계약 형태와 똑같이 생긴 가짜 응답 데이터를 대신 써두는 거예요.
- **원자적(atomic) 처리**: 여러 단계로 이루어진 작업이 "전부 성공" 아니면 "전부 실패"만 있고, 중간 상태로 남지 않는다는 뜻이에요. 예를 들어 재고를 차감하는 도중 오류가 나면, 재고는 아예 안 줄어든 것처럼 되돌아가야 해요 — 반만 줄어든 채로 남으면 안 돼요.
- **감사 로그(audit log)**: "누가, 언제, 무엇을 했는지" 기록을 남기는 거예요. 나중에 재고 수량이 왜 이렇게 됐는지 추적할 때 필요해요.
- **멱등성(idempotency)**: 같은 요청/작업을 실수로 여러 번 반복해도 결과가 한 번 했을 때랑 똑같다는 뜻이에요. 예를 들어 정산을 같은 기간에 대해 두 번 돌려도 금액이 두 배로 뻥튀기되면 안 돼요.
- **불변(immutable) 스냅샷**: 한 번 만들어지면 나중에 절대 수정되지 않는 기록이에요. 정산명세서는 "그 시점에 이랬다"는 증거라서, 나중에 판매 기록이 바뀌어도 이미 나간 정산명세서 자체는 안 바뀌어요.
- **스파이크(spike)**: 코드를 완성하는 게 아니라, 구현하기 전에 애매한 부분을 먼저 조사/설계해서 문서로 정리하는 작업이에요.

## 진행 상태

| Phase | 상태 |
| --- | --- |
| 0. 인덱스 페이지 | 진행 중 (Figma) |
| 0.5. Flyway 도입 + 기본 CI | 예정 |
| 1. 창작자 | 예정 |
| 2. 위탁계약 (+ Settlement 설계 스파이크 시작) | 예정 |
| 3. 입고배치 | 예정 |
| 4A. 판매 | 예정 |
| 4B. 폐기기록 | 예정 |
| 5. 정산명세서 | 예정 |
